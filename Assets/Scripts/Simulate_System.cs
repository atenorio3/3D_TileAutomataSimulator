using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Note to Self: I will use dictionaries to track the system's tile placements (Space) and moves (Time).
public class Simulate_System : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
